# Rust build output for this Cargo workspace
target/

# Local sqlite databases created at runtime
*.db
*.db-shm
*.db-wal

# Editor / OS artifacts (mirrors of root .gitignore, kept for safety if the
# `server/` directory is ever cloned or vendored on its own)
.idea/
.vscode/
.DS_Store
Thumbs.db
