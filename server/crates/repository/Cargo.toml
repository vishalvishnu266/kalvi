[package]
name = "repository"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite"] }
tokio = { version = "1", features = ["full"] }
