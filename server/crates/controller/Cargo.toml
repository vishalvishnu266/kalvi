[package]
name = "controller"
version = "0.1.0"
edition = "2024"

[dependencies]
model = { path = "../model" }
repository = { path = "../repository" }
service = { path = "../service" }
web = { path = "../web" }
axum = "0.8"
sqlx = { version = "0.8", features = ["sqlite"] }
