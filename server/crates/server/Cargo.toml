[package]
name = "server"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
tenant = { path = "../tenant" }
auth = { path = "../auth" }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
